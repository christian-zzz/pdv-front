import React from 'react';
import HeroSlider from '../components/home/HeroSlider';

const Home = () => {
    return (
        <div>
            <HeroSlider />
            {/* Future sections (Featured, Offers, etc.) will go here */}
        </div>
    );
};

export default Home;
